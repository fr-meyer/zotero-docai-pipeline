defaults:
  - base_config
  - zotero: default
  - ocr: pageindex
  - processing: default
  - storage: default
  - tree_structure: default
  - download: default
  - tag_adding: default
  - tagging: default
  - export: default
  - credentials: default
  - _self_

# Hydra runtime configuration
hydra:
  job:
    chdir: false
