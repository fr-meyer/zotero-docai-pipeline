# Credentials are injected via os.getenv() in build_app_config().
# Do NOT set real key values here — they would appear in Hydra config dumps.
library_id: "__PACKAGED_PLACEHOLDER_LIBRARY_ID__"
read_key: "__PACKAGED_PLACEHOLDER_READ_KEY__"
write_key: null
redact_logs: true
