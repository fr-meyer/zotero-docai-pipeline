# Python dependencies for zotero-docai-pipeline
# This file mirrors the pip dependencies from environment.yml
# For conda-based installation, use: conda env create -f environment.yml

# Core dependencies (pip packages from environment.yml)
pyzotero
mistralai<2
tabulate
pageindex
markdown-it-py
mdit-py-plugins
hydra-core
omegaconf